apiVersion: v1
kind: Service
metadata: 
  name: backend-service
spec:
  selector:
    app: task-management-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
  type: NodePort
